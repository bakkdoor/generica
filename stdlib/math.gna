(define <=
  (lambda (x y)
    (if (= x y)
        t
      (< x y))))

(define >=
  (lambda (x y)
    (if (= x y)
        t
      (> x y))))

(define even?
  (lambda (x)
    (= (% x 2) 0)))

(define odd?
  (lambda (x)
    (not (even? x))))